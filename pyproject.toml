[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "workforest"
dynamic = ["version"]
description = "Git worktree forest management: create, open, and clean up per-branch worktrees with project-defined setup hooks"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Arkady Buryakov", email = "arkady@buryakov.pro" }]
requires-python = ">=3.14"
dependencies = ["pyyaml>=6.0"]
keywords = ["git", "worktree", "cli", "developer-tools"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Version Control :: Git",
]

[project.urls]
Homepage = "https://github.com/ArkadyBuryakov/workforest"

[project.scripts]
workforest = "workforest.cli:main"
wf = "workforest.cli:main"

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-cov>=6.0",
    "ruff>=0.8",
    "mypy>=1.13",
    "types-pyyaml>=6.0",
]

[tool.hatch.version]
path = "src/workforest/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/workforest"]

# Installed under <prefix>/share/man by pip/uv/installer: /usr/share/man for
# the Arch package, the venv for `uv tool`/pipx (shell-init then puts it on
# $MANPATH), libexec for the Homebrew formula (which moves them out again).
[tool.hatch.build.targets.wheel.shared-data]
"man/workforest.1" = "share/man/man1/workforest.1"
"man/wf.1" = "share/man/man1/wf.1"
"man/workforest.5" = "share/man/man5/workforest.5"
"man/wf.5" = "share/man/man5/wf.5"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=workforest --cov-report=term-missing --cov-fail-under=90"

[tool.coverage.run]
branch = true
source = ["workforest"]

[tool.coverage.report]
exclude_also = ["if TYPE_CHECKING:", "raise NotImplementedError"]

[tool.mypy]
strict = true
files = ["src/workforest"]
python_version = "3.14"

[tool.ruff]
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E", "W",   # pycodestyle
    "F",        # pyflakes
    "I",        # isort
    "UP",       # pyupgrade
    "B",        # bugbear
    "SIM",      # simplify
    "PTH",      # pathlib
    "RUF",
]
