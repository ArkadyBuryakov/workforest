# Only the compiled extension, its manifest, the docs, and (in the
# platform-specific builds) bin/workforest ship in the .vsix.
.vscode/**
src/**
out/test/**
out/smoke/**
scripts/**
out/**/*.map
node_modules/**
tsconfig.json
*.vsix
